server:
  port: ${PORT:8080}
  shutdown: graceful

spring:
  profiles:
    active: local
    include:
        - jdbc

---

logging:
  level:
    org:
      springframework:
        jdbc:
          core:
            JdbcTemplate: DEBUG

spring:
  config:
    activate:
      on-profile: local

---

spring:
  config:
    activate:
      on-profile: prod