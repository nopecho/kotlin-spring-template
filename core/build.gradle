subprojects {
    dependencies {
        implementation(project(":common:common-utils"))
        testApi(testFixtures(project(":common:common-utils")))
    }

    bootJar {
        enabled = false
    }
}

bootJar {
    enabled = false
}