subprojects {
    dependencies {
        implementation(project(":common:common-core"))
        testApi(testFixtures(project(":common:common-core")))
    }

    bootJar {
        enabled = false
    }
}

bootJar {
    enabled = false
}